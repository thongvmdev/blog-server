version: "3.8"
services:
  backend:
    image: ghcr.io/REPLACE_REPOSITORY:REPLACE_SHA
    labels:
      - "version=REPLACE_SHA"
    container_name: container-blog-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env

# networks:
#   nginx_network:
#     external: true



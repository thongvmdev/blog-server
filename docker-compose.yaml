version: "3.8"
services:
  backend:
    image: $IMAGE_TAG
    container_name: container-blog-server
    build:
      context: .
      dockerfile: Dockerfile
      args:
        IMAGE_TAG: ${IMAGE_TAG}
        LABELS: ${LABELS}
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env

# networks:
#   nginx_network:
#     external: true


